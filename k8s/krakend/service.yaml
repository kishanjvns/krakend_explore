apiVersion: v1
kind: Service
metadata:
  name: krakend-service
  namespace: mediq
spec:
  type: NodePort
  selector:
    app: krakend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080

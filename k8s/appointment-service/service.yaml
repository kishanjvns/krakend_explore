apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  namespace: mediq
spec:
  type: ClusterIP
  selector:
    app: appointment-service
  ports:
    - port: 8084
      targetPort: 8084
